# AI 测试用例生成器 - 使用操作文档

## 一、工具说明

本工具用于将 Word 格式的需求文档（.docx）自动生成测试用例，并导出为 Excel 文件。通过 AI 大模型分析需求内容，自动覆盖正常流程、异常输入、边界条件、权限控制等多种测试场景。

## 二、使用前准备

### 2.1 获取工具

从同事处拷贝整个 `ai测试` 文件夹到本地任意目录即可。

### 2.2 安装 Python 环境

如果电脑上没有 Python，需要先安装：

1. 打开浏览器访问 https://www.python.org/downloads/
2. 下载 Python 3.11 或更高版本
3. 安装时务必勾选 **「Add Python to PATH」**（添加到环境变量）
4. 安装完成后，打开命令提示符（Win+R 输入 cmd 回车），输入以下命令验证：
   ```
   python --version
   ```
   看到类似 `Python 3.11.x` 的输出即表示安装成功。

### 2.3 安装依赖包

1. 打开命令提示符
2. 进入工具目录，例如：
   ```
   cd G:\ai测试
   ```
3. 执行安装命令：
   ```
   pip install -r requirements.txt
   ```
4. 等待安装完成（首次安装可能需要几分钟）

### 2.4 配置 API Key

1. 在工具目录下找到 `env.sample` 文件
2. 复制一份，重命名为 `.env`
3. 用记事本打开 `.env`，修改以下内容：

```
DEEPSEEK_API_KEY=sk-你的API密钥
```

> API Key 请找项目负责人获取。其他配置项一般不需要修改。

## 三、操作步骤

### 3.1 启动工具

1. 打开命令提示符
2. 进入工具目录：
   ```
   cd G:\ai测试
   ```
3. 启动程序：
   ```
   python main.py
   ```
4. 等待几秒后会弹出图形界面窗口

### 3.2 界面说明

程序界面分为左右两部分：

```
┌─────────────────────┬──────────────────────────┐
│                     │                          │
│    需求文档列表      │     运行配置              │
│                     │                          │
│  [添加文件] [清空]   │  API Key: ****           │
│                     │  Base URL: ...           │
│                     │  模型: deepseek-chat      │
│                     │  项目名称: 蓝点新生        │
│                     │  模块名称: 采购管理        │
│                     │  子模块名称: 废品回收      │
│                     │  分段大小: 3000           │
│                     │  □ 启用OCR               │
│                     │  ☑ 汇总到一个Excel        │
│                     │  输出目录: output [浏览]   │
│                     │                          │
│                     │  [开始生成]               │
│                     │                          │
│                     │  实时日志                  │
│                     │  ________________________ │
│                     │ |                        |│
│                     │ |  处理进度会显示在这里    |│
│                     │ |________________________|│
└─────────────────────┴──────────────────────────┘
```

### 3.3 生成测试用例

**第一步：添加需求文档**

1. 点击左侧「添加文件」按钮
2. 在弹出的文件选择框中，选择一个或多个 .docx 格式的需求文档
3. 选中的文件会显示在左侧列表中
4. 如需重新选择，点击「清空」后重新添加

**第二步：填写配置信息**

右侧配置区域需要关注以下几项：

| 配置项 | 说明 | 是否必填 |
|--------|------|---------|
| API Key | AI 接口密钥（.env 配好后会自动填入） | 是 |
| 项目名称 | 写入 Excel 的项目名称列 | 建议填写 |
| 模块名称 | 写入 Excel 的模块名称列 | 建议填写 |
| 子模块名称 | 写入 Excel 的子模块名称列，同时作为 Sheet 名 | 建议填写 |
| 启用 OCR | 需求文档中有截图时勾选，会识别图片中的文字 | 按需 |
| 汇总到一个 Excel | 勾选后所有文档的用例写入同一个 Excel，按子模块分 Sheet | 推荐勾选 |
| 输出目录 | 生成的 Excel 保存位置 | 默认 output 目录 |

> 其他配置项（Base URL、模型、分段大小等）保持默认即可。

**第三步：开始生成**

1. 确认文件和配置无误后，点击「开始生成」
2. 底部日志区域会实时显示进度：
   - `📄 开始处理：xxx.docx` — 正在读取文档
   - `🧠 调用 AI 生成（xxx.docx 分段 1/2）` — 正在调用 AI
   - `✅ xxx.docx 完成，共 15 条` — 该文档处理完成
   - `🎉 全部文件处理完成` — 所有文档处理完毕
3. 生成过程中请耐心等待，每个文档大约需要 1-3 分钟（取决于文档长度和网络速度）
4. 生成完成后，到输出目录查看结果文件

**第四步：查看结果**

生成完成后，在输出目录（默认 `output` 文件夹）中会看到：

| 文件 | 说明 |
|------|------|
| `汇总测试用例.xlsx` | 所有文档的测试用例汇总（汇总模式） |
| `{文件名}_测试用例.xlsx` | 单个文档的测试用例（非汇总模式） |
| `{文件名}_AI原始输出.txt` | AI 返回的原始文本，用于核对 |

### 3.4 Excel 输出格式

生成的 Excel 包含以下列：

| 列名 | 内容示例 |
|------|---------|
| 序号 | 1, 2, 3... |
| 项目名称 | 蓝点新生 |
| 模块名称 | 采购管理 |
| 子模块名称 | 废品回收 |
| 功能点 | 字段默认值验证 |
| 用例标题 | 验证通过废品回收接单生成采购订单时字段默认值及状态正确 |
| 前置条件 | 用户已登录系统，具有采购管理权限 |
| 输入数据 | — |
| 操作步骤 | Step1. 进入采购管理模块... Step2. 点击新增... |
| 预期结果 | 系统自动生成采购订单，状态为"草稿" |

- 操作步骤或预期结果为空的单元格会自动标黄，方便人工补充
- 同一个 Sheet 多次生成会自动追加，序号自动重排

## 四、多文档批量处理

如果有多个需求文档需要一次性处理：

1. 点击「添加文件」时按住 Ctrl 键多选，或多次点击添加
2. 勾选「汇总到一个 Excel」
3. 每个文档处理前，修改右侧的「模块名称」和「子模块名称」
4. 或者每次只添加一个文档，修改模块信息后生成，再添加下一个

> 汇总模式下，每个文档的用例会写入以「子模块名称」命名的 Sheet 中。

## 五、常见问题

### Q：点击「开始生成」后没反应
- 检查是否已添加文件
- 检查 API Key 是否已填写
- 查看底部日志是否有错误信息

### Q：提示 API 调用失败
- 检查网络连接是否正常
- 确认 API Key 是否正确且未过期
- 程序会自动重试 3 次，如仍失败请稍后再试

### Q：提示文件被占用，无法写入
- 关闭正在打开的 Excel 文件
- 关闭 WPS 或 Microsoft Excel 程序
- 重新点击「开始生成」

### Q：生成的用例数量太少
- 检查需求文档内容是否过于简短
- 适当减小「分段大小」（如改为 2000），让 AI 更细致地分析每段内容

### Q：需求文档中有截图，AI 没有识别
- 勾选「启用 OCR」选项
- 首次启用 OCR 需要下载模型，请确保网络畅通
- OCR 语言默认中文（ch），如有英文截图可改为 en

### Q：启动程序报错 "No module named xxx"
- 说明依赖包未安装完整，重新执行：
  ```
  pip install -r requirements.txt
  ```

### Q：如何更换 AI 模型
- 在界面右侧「模型」输入框中修改模型名称
- 如使用其他 AI 服务商，同时修改「Base URL」为对应的 API 地址
