# AI测试用例生成工具 - 新流程使用说明

## 架构升级概述

本次升级将原来的单阶段生成改为两阶段生成：

### 原流程
```
需求文档 → AI直接生成测试用例 → 导出Excel
```

### 新流程  
```
需求文档 → 解析需求文本 → AI生成测试点 → AI根据测试点生成测试用例 → 导出Excel
```

## 新增模块

### 1. 测试点生成器 (`app/test_design/testpoint_generator.py`)
- **功能**：根据需求文本生成结构化测试点列表
- **输入**：需求文档文本
- **输出**：测试点列表，如 `["验证订单创建功能", "验证订单金额边界条件", "验证订单状态异常处理"]`
- **覆盖维度**：
  - 功能测试：核心业务功能验证
  - 边界测试：输入边界值、数量限制等
  - 异常测试：错误处理、网络异常等
  - 权限测试：角色权限、访问控制等
  - 数据校验：必填字段、格式校验等

### 2. 测试用例生成器 (`app/test_design/testcase_generator.py`)
- **功能**：根据测试点生成详细测试用例
- **输入**：测试点列表
- **输出**：结构化测试用例，包含：
  - `test_point`: 对应的测试点
  - `title`: 用例标题
  - `precondition`: 前置条件
  - `steps`: 操作步骤
  - `expected`: 预期结果
  - `priority`: 优先级

### 3. 新工作流程 (`app/core/new_workflow.py`)
- **功能**：整合两阶段生成流程
- **特性**：
  - 支持大文档分块处理
  - 自动去重和质量分析
  - 生成测试点中间文件
  - 完整的错误处理和日志

## Excel结构升级

### 新增字段
- **测试点**：显示对应的测试点描述

### 最终Excel列结构
1. 序号
2. 项目名称
3. 模块名称
4. 子模块名称
5. **测试点** (新增)
6. 用例标题
7. 前置条件
8. 操作步骤
9. 预期结果
10. 优先级

## 使用方法

### 1. 命令行测试
```bash
# 测试新流程
python test_new_workflow.py 需求文档.docx

# 使用原main.py的演示功能
python main.py 需求文档.docx
```

### 2. 程序化调用
```python
from app.config import load_config
from app.core.new_workflow import generate_with_new_workflow
from pathlib import Path

# 加载配置
config = load_config()

# 执行新流程
results = generate_with_new_workflow(
    files=[Path("需求文档.docx")],
    config=config,
    log_callback=lambda msg: print(msg)
)
```

### 3. GUI界面
- 现有GUI界面保持不变
- 可以通过配置选择使用新流程或原流程

## 输出文件

### 1. 测试点文件
- 文件名：`{文档名}_测试点.txt`
- 内容：编号的测试点列表
- 用途：中间结果，便于审查和调整

### 2. Excel测试用例
- 文件名：`{文档名}_测试用例_新流程.xlsx`
- 包含完整的测试用例信息
- 新增"测试点"列

### 3. 覆盖率统计
- 工作表：`覆盖率统计`
- 包含质量评分、维度分布等信息

## 优势

### 1. 更好的可控性
- 可以先审查测试点，再生成用例
- 便于调整测试覆盖范围

### 2. 更高的质量
- 两阶段生成更加系统化
- 测试点设计更全面

### 3. 更好的可追溯性
- 每个测试用例都对应明确的测试点
- 便于理解测试意图

### 4. 向后兼容
- 保持原有接口不变
- 支持新旧格式混用

## 配置要求

确保 `.env` 文件包含必要的API配置：
```
DEEPSEEK_API_KEY=your_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
```

## 注意事项

1. **API调用次数**：新流程会增加API调用次数（测试点生成 + 测试用例生成）
2. **处理时间**：相比原流程会增加一些处理时间
3. **文件输出**：会生成额外的测试点中间文件
4. **兼容性**：完全兼容现有的配置和输出格式