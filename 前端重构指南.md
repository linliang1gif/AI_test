# 🎨 前端重构指南

## 📋 重构目标

将前端类型定义与后端 core 层完全对齐，实现前后端类型一致性。

---

## ✅ 已完成工作

### 1. 创建类型定义文件

```
ai-test-platform/frontend/src/types/
├── core.ts          # 核心数据模型（与 core/models.py 对齐）
├── enums.ts         # 枚举映射和辅助函数
└── api.ts           # API 请求响应类型
```

### 2. 创建通用组件

```
ai-test-platform/frontend/src/components/
└── DataTypeTag.tsx  # 数据类型和预期行为标签组件
```

---

## 📋 待完成工作

### 1. 更新 TestCases.jsx

需要在测试用例详情对话框中显示新字段：

```jsx
// ai-test-platform/frontend/src/pages/TestCases.jsx

import { DataTypeTag, ExpectedBehaviorTag } from '../components/DataTypeTag';

// 在详情对话框中添加：
<div className="grid grid-cols-2 gap-4 mb-4">
  <div>
    <label className="block text-sm font-medium text-gray-700 mb-1">数据类型</label>
    <div className="p-3 bg-gray-50 rounded border">
      <DataTypeTag dataType={selectedTestCase.data_type} />
    </div>
  </div>
  <div>
    <label className="block text-sm font-medium text-gray-700 mb-1">预期行为</label>
    <div className="p-3 bg-gray-50 rounded border">
      <ExpectedBehaviorTag behavior={selectedTestCase.expected_behavior} />
    </div>
  </div>
</div>
```

### 2. 更新表格显示（可选）

在测试用例列表表格中添加新列：

```jsx
<th>数据类型</th>
<th>预期行为</th>

// 在表格行中：
<td><DataTypeTag dataType={tc.data_type} /></td>
<td><ExpectedBehaviorTag behavior={tc.expected_behavior} /></td>
```

### 3. 更新 TestRuns.jsx

显示修复信息：

```jsx
{result.healing_applied && (
  <div className="mt-2 p-2 bg-blue-50 rounded">
    <span className="text-sm text-blue-700">
      🔧 已应用修复: {result.healing_level}
    </span>
  </div>
)}
```

### 4. 更新 Reports.jsx

显示修复统计：

```jsx
{report.healing_summary && Object.keys(report.healing_summary).length > 0 && (
  <div className="mt-4">
    <h3 className="font-medium mb-2">修复统计</h3>
    <div className="grid grid-cols-4 gap-2">
      {Object.entries(report.healing_summary).map(([level, count]) => (
        <div key={level} className="p-2 bg-gray-50 rounded">
          <div className="text-xs text-gray-500">{level}</div>
          <div className="text-lg font-bold">{count}</div>
        </div>
      ))}
    </div>
  </div>
)}
```

---

## 🔧 服务层更新

### 创建统一的服务层

```typescript
// ai-test-platform/frontend/src/services/testcase.service.ts

import api from './api';
import { TestCase } from '../types/core';
import { GenerateTestCasesResponse, GetTestCasesResponse } from '../types/api';

export const testcaseService = {
  async generate(file: File): Promise<GenerateTestCasesResponse> {
    const formData = new FormData();
    formData.append('file', file);
    
    const response = await api.post('/api/testcases/generate', formData, {
      headers: { 'Content-Type': 'multipart/form-data' }
    });
    
    return response.data;
  },

  async getAll(): Promise<GetTestCasesResponse> {
    const response = await api.get('/api/test-cases');
    return response.data;
  },

  async getById(id: string): Promise<TestCase> {
    const response = await api.get(`/api/test-cases/${id}`);
    return response.data;
  },

  async batchDelete(ids: number[]): Promise<any> {
    const response = await api.post('/api/testcases/batch-delete', { ids });
    return response.data;
  }
};
```

---

## 📦 组件更新清单

### 必须更新的组件

- [ ] `TestCases.jsx` - 添加新字段显示
- [ ] `TestRuns.jsx` - 显示修复信息
- [ ] `Reports.jsx` - 显示修复统计

### 可选更新的组件

- [ ] `Dashboard.jsx` - 添加数据类型统计
- [ ] `ApiExplorer.jsx` - 显示 API 数据类型
- [ ] `DatasetManagement.jsx` - 数据集类型标签

---

## 🎯 类型使用示例

### 在组件中使用类型

```typescript
import { TestCase, TestCaseStatus, DataType } from '../types/core';
import { DataTypeLabels, StatusColors } from '../types/enums';

interface TestCaseCardProps {
  testCase: TestCase;
}

const TestCaseCard: React.FC<TestCaseCardProps> = ({ testCase }) => {
  const statusColor = StatusColors[testCase.status];
  const dataTypeLabel = DataTypeLabels[testCase.data_type];
  
  return (
    <div className="p-4 border rounded">
      <h3>{testCase.title}</h3>
      <span className={statusColor}>{testCase.status}</span>
      <span>{dataTypeLabel}</span>
    </div>
  );
};
```

### 在 API 调用中使用类型

```typescript
import { testcaseService } from '../services/testcase.service';
import { TestCase } from '../types/core';

const loadTestCases = async () => {
  try {
    const response = await testcaseService.getAll();
    const testCases: TestCase[] = response.data;
    setTestCases(testCases);
  } catch (error) {
    console.error('加载失败:', error);
  }
};
```

---

## ✅ 验证清单

### 类型定义

- [x] core.ts 创建完成
- [x] enums.ts 创建完成
- [x] api.ts 创建完成

### 组件

- [x] DataTypeTag.tsx 创建完成
- [ ] TestCases.jsx 更新
- [ ] TestRuns.jsx 更新
- [ ] Reports.jsx 更新

### 服务层

- [ ] testcase.service.ts 创建
- [ ] execution.service.ts 创建
- [ ] report.service.ts 创建
- [ ] data.service.ts 创建

### 测试

- [ ] 类型检查通过
- [ ] 组件渲染正常
- [ ] API 调用正常
- [ ] 端到端测试通过

---

## 🚀 执行步骤

### 步骤1: 安装 TypeScript（如果需要）

```bash
cd ai-test-platform/frontend
npm install --save-dev typescript @types/react @types/react-dom
```

### 步骤2: 更新组件

逐个更新组件文件，添加类型导入和新字段显示。

### 步骤3: 创建服务层

创建统一的服务层文件，封装所有 API 调用。

### 步骤4: 测试验证

```bash
# 启动开发服务器
npm run dev

# 访问 http://localhost:5173
# 测试所有功能
```

---

## 📝 注意事项

1. **类型一致性**: 确保前端枚举值与后端完全一致
2. **字段命名**: 使用 snake_case（与后端一致）
3. **可选字段**: 使用 `?` 标记可选字段
4. **日期格式**: 统一使用 ISO 8601 格式字符串
5. **错误处理**: 所有 API 调用都要有错误处理

---

## 🎉 预期效果

重构完成后：

1. ✅ 前后端类型完全对齐
2. ✅ TypeScript 类型检查
3. ✅ 更好的代码提示
4. ✅ 减少运行时错误
5. ✅ 统一的数据结构

---

**重构负责人**: 前端架构师  
**重构日期**: 2024-03-24  
**重构状态**: 🟡 进行中

