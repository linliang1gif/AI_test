# 当前项目状态总结

## 一、已完成的功能模块

### ✅ P0-8.1：环境管理 + 鉴权配置（已完成）
**位置**：`/projects-v2/:id` → "环境配置" Tab

**功能**：
- 环境 CRUD 操作（创建、查看、编辑、删除）
- 6 种鉴权类型支持（none, bearer, apikey, basic, oauth2, custom）
- 默认请求头配置
- 环境类型管理（dev/test/staging/prod）
- 保护环境和写操作权限控制

**文件**：
- `frontend/src/components/EnvironmentManager.jsx`
- `frontend/src/components/EnvironmentForm.jsx`
- `frontend/src/components/AuthProfileForm.jsx`
- `frontend/src/pages/ProjectDetailV2.jsx`（已集成）

**API**：全部使用 V2 API，无 Pilot API 依赖

---

### ✅ AI 模型配置功能（刚完成）
**位置**：`/ai-config`（侧边栏 → 配置管理 → AI 模型配置）

**功能**：
- 查看 AI 提供商状态（DeepSeek、智谱 BigModel、Ollama）
- 模块级别的 AI 配置（4 个模块）
  - 测试用例生成
  - 测试脚本生成
  - Swagger 分析
  - 测试优化
- 可视化配置界面
- 配置持久化到 `.env`

**文件**：
- `frontend/src/pages/AIConfigPage.jsx`（新增）
- `config/config.py`（已更新）
- `ai/ai_client.py`（已更新）
- `routes/ai_routes.py`（已更新）
- `.env`（已更新）

**API**：
- GET `/api/v1/ai/providers`
- GET `/api/v1/ai/config`
- POST `/api/v1/ai/config/update`

---

## 二、当前可访问的页面（12 个）

### 项目管理
1. `/projects-v2` - 项目列表
2. `/projects-v2/:projectId` - 项目详情（含环境管理）
3. `/test-cases` - 测试用例列表

### 测试开发
4. `/api-specs` - API 规范列表
5. `/api-specs/:id` - API 规范详情
6. `/swagger-workbench` - Swagger 接入工作台
7. `/dataset-management` - 数据集管理
8. `/test-data-factory` - 测试数据工厂

### 执行管理
9. `/test-runs-v2` - 执行记录
10. `/test-runs-v2/:runId` - 执行详情
11. `/quick-execution-test` - 快速执行测试

### 配置管理
12. `/ai-config` - AI 模型配置（新增）

---

## 三、V2 主线建设进度

### 已完成
- ✅ P0-8.1：环境管理 + 鉴权配置
- ✅ AI 模型配置功能

### 待完成（按优先级）

#### 🎯 P0-8.2：测试用例详情页 V2（下一步）
**目标**：补齐测试用例的详情查看能力

**必须功能**：
- 查看测试用例基本信息（标题、描述、优先级）
- 查看来源（swagger / ai_generated / manual）
- 查看请求参数（method, path, headers, body）
- 查看预期结果（expected_status, expected_response）
- 查看数据类型（data_type）
- 从详情页触发执行
- 查看执行历史

**API**：
- GET `/api/v2/test-cases/:id`
- POST `/api/v2/test-cases/:id/execute`
- GET `/api/v2/test-cases/:id/runs`

**路由**：`/test-cases/:id`

**复用组件**：
- 可能需要新建 `TestCaseDetailV2.jsx`
- 复用 `ApiExecutor.jsx` 用于执行

---

#### 🎯 P0-8.3：API 管理 V2
**目标**：从 API 规范查看和管理具体 API

**必须功能**：
- 从 API 规范详情页查看 API 列表
- 查看单个 API 详情（path, method, parameters, responses）
- 支持 API 测试（发送请求）
- 从 API 生成测试用例
- 查看 API 的测试用例

**API**：
- GET `/api/v2/swagger/api-specs/:id/apis`
- GET `/api/v2/apis/:id`
- POST `/api/v2/apis/:id/test`
- POST `/api/v2/apis/:id/generate-test-cases`

**路由**：
- `/api-specs/:specId/apis`（API 列表）
- `/apis/:id`（API 详情）

---

#### 🎯 P0-8.4：测试执行增强
**目标**：增强测试执行能力

**必须功能**：
- 批量执行测试用例
- 执行进度实时显示
- 执行结果详细展示
- 失败重试
- 执行报告导出

---

## 四、严格限制（重要！）

### ❌ 不要做的事情
1. **不要恢复旧页面**
   - 不要恢复依赖 `/api/pilot/*` 的页面
   - 不要恢复 28 个未开放的旧页面

2. **不要做这些模块**（现在不做）
   - Dashboard / 仪表盘
   - Reports / 报告中心
   - AI Insights / AI 洞察
   - Settings / 系统设置
   - 旧版 Projects / ApiExplorer / TestRuns

3. **不要制造多版本**
   - 不要做 Pro / Simple / List 多版本
   - 不要做独立的复杂系统

4. **不要做 UI 美化优先**
   - 功能优先，美化其次
   - 先能用，再好看

### ✅ 必须遵守的原则
1. **只使用 V2 API**
   - 所有新功能必须使用 `/api/v2/*`
   - 不触发任何 `/api/pilot/*` 请求

2. **补齐主线能力**
   - 专注于核心测试流程
   - 项目 → 环境 → API → 测试用例 → 执行 → 结果

3. **最小可用**
   - 每个功能做到最小可用即可
   - 不过度设计

4. **复用优先**
   - 优先复用现有组件
   - 优先在现有页面扩展

---

## 五、技术栈和架构

### 后端
- **框架**：FastAPI
- **数据库**：SQLite（通过 SQLAlchemy）
- **API 版本**：V2（`/api/v2/*`）
- **AI 集成**：支持 DeepSeek、智谱 BigModel、Ollama

### 前端
- **框架**：React 18
- **路由**：React Router v6
- **构建工具**：Vite
- **UI 组件**：自定义组件（Card, Toast, Modal 等）
- **状态管理**：React Hooks（useState, useEffect）

### 开发环境
- **后端端口**：8000
- **前端端口**：5173
- **Python 命令**：`py`（不是 `python`）
- **启动脚本**：
  - 后端：`cd ai-test-platform && py backend_api_server.py`
  - 前端：`cd ai-test-platform/frontend && npm run dev`

---

## 六、当前数据状态

### 蓝点项目接入数据
- **项目 ID**：8（蓝点回收系统）
- **环境 ID**：1（test）
- **鉴权 ID**：1（Bearer Token）
- **API Spec ID**：7（817 个 API）

### AI 配置
- **默认提供商**：deepseek
- **默认模型**：deepseek-chat
- **可用提供商**：3 个（DeepSeek、智谱 BigModel、Ollama）

---

## 七、下一步行动计划

### 立即开始：P0-8.2 测试用例详情页 V2

#### 1. 文件改动计划（先输出）
- 需要新增哪些文件
- 需要修改哪些文件
- 需要新增哪些路由
- 需要调用哪些 API

#### 2. 实施步骤
1. 检查后端 API 是否完整
2. 创建 `TestCaseDetailV2.jsx` 页面
3. 添加路由 `/test-cases/:id`
4. 实现基本信息展示
5. 实现执行功能
6. 实现执行历史查看
7. 测试验证

#### 3. 验收标准
- ✅ 能从测试用例列表点击进入详情页
- ✅ 能查看测试用例完整信息
- ✅ 能看到来源标识（swagger/ai/manual）
- ✅ 能看到请求参数和预期结果
- ✅ 能从详情页触发执行
- ✅ 能查看执行历史
- ✅ 不触发任何 `/api/pilot/*` 请求

---

## 八、关键提醒

1. **用 `py` 不是 `python`**
   - 你的系统用 `py` 命令运行 Python

2. **前端用 Vite**
   - 启动命令是 `npm run dev` 不是 `npm start`

3. **端口配置**
   - 后端：8000
   - 前端：5173

4. **API 前缀**
   - V2 API：`/api/v2/*`
   - V1 API：`/api/v1/*`（AI 相关）
   - 禁止：`/api/pilot/*`

5. **环境变量**
   - 修改 `.env` 后需要重启后端
   - DeepSeek API Key：`sk-56a8ef734a534e0a9f4f1d4787f29e6d`

---

## 九、总结

当前项目处于 V2 主线建设阶段：
- ✅ 已完成：环境管理、鉴权配置、AI 模型配置
- 🎯 下一步：测试用例详情页 V2（P0-8.2）
- 📋 后续：API 管理 V2（P0-8.3）、测试执行增强（P0-8.4）

**核心目标**：补齐 V2 主线最缺的页面能力，不恢复旧壳子，不做过度设计。

**工作原则**：最小可用、复用优先、V2 API Only、功能优先。
