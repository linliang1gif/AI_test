# Skill 要求更新说明

**更新时间**: 2024-03-24  
**原因**: 根据 Skill 文档的实际要求调整生成数量

---

## 📋 Skill 文档要求

根据 `ai-test-platform/skills/test-case-generator/SKILL.md`：

### 数量要求
- ✅ **每个模块至少生成 100个 测试用例**
- ✅ **每个测试点至少生成 20个 场景**
- ✅ **每个场景至少生成 5个 用例**

### 测试维度（必须覆盖）
1. **功能测试** - 正常流程、核心功能、业务规则
2. **边界测试** - 最小值/最大值、临界值、空值、超长数据
3. **异常测试** - 非法输入、错误参数、异常状态
4. **安全测试** - SQL注入、XSS攻击、权限控制
5. **性能测试** - 响应时间、并发处理
6. **兼容性测试** - 不同浏览器、设备、分辨率

### 质量标准
- 测试步骤：至少 5 步，每步具体可执行
- 测试数据：具体示例值（如 `test@example.com`），不要写"有效数据"
- 标题：简洁明确，不超过30字

---

## 🔄 更新内容

### 更新前（不符合 Skill 要求）
```python
# 简单需求: 10-15 个
# 中等需求: 20-30 个
# 复杂需求: 30-50 个
```

### 更新后（符合 Skill 要求）
```python
# 估算模块数量
estimated_modules = 识别需求中的模块数量

# 简单需求（< 3000字符）
target_count = f"{estimated_modules * 30}-{estimated_modules * 50}个"
max_tokens = 6000 + (estimated_modules * 1000)

# 中等需求（3000-5000字符）
target_count = f"{estimated_modules * 50}-{estimated_modules * 100}个"
max_tokens = 8000 + (estimated_modules * 1000)

# 复杂需求（> 5000字符）
target_count = f"{estimated_modules * 100}-{estimated_modules * 150}个"
max_tokens = 12000 + (estimated_modules * 2000)

# 最大限制: 16000 tokens
```

---

## 📊 生成数量对比

### 单模块需求

| 需求复杂度 | 更新前 | 更新后 | 符合Skill |
|-----------|--------|--------|----------|
| 简单（< 3000字符） | 10-15个 | 30-50个 | ✅ |
| 中等（3000-5000字符） | 20-30个 | 50-100个 | ✅ |
| 复杂（> 5000字符） | 30-50个 | 100-150个 | ✅ |

### 多模块需求（如 v1.2.2）

假设识别到 **5个模块**：

| 需求复杂度 | 更新前 | 更新后 | 符合Skill |
|-----------|--------|--------|----------|
| 简单 | 10-15个 | 150-250个 | ✅ |
| 中等 | 20-30个 | 250-500个 | ✅ |
| 复杂 | 30-50个 | 500-750个 | ✅ |

---

## 🎯 v1.2.2 需求示例

你的 v1.2.2 需求文档包含：

### 识别的模块
1. 付款单模块
2. 企业管理模块
3. 财务应付单模块
4. 供应商管理模块
5. 采购订单模块
6. 反向开票申请单模块

**估算**: 6个模块

### 预期生成数量
- 需求长度: ~5000+ 字符（复杂需求）
- 模块数量: 6个
- **目标生成**: 600-900 个测试用例
- **max_tokens**: 12000 + (6 * 2000) = 24000 → 限制为 16000

---

## 🔧 技术实现

### 1. 模块数量估算
```python
# 简单启发式：统计关键词出现次数
module_keywords = ['模块', '功能', '页面', '接口', '服务']
estimated_modules = sum(1 for keyword in module_keywords if keyword in content[:1000])
estimated_modules = max(1, estimated_modules)  # 至少1个模块
```

### 2. 动态调整 Token 限制
```python
if content_length > 5000:
    max_tokens = 12000 + (estimated_modules * 2000)
elif content_length > 3000:
    max_tokens = 8000 + (estimated_modules * 1000)
else:
    max_tokens = 6000

# 限制最大值
max_tokens = min(max_tokens, 16000)
```

### 3. 增强的 Prompt
```python
prompt = f"""
## 生成要求（重要！）:
根据 Skill 规范，必须生成 {target_count} 高质量测试用例。

测试用例必须覆盖以下维度:
1. 功能测试 - 正常流程、核心功能、业务规则
2. 边界测试 - 最小值/最大值、临界值、空值、超长数据
3. 异常测试 - 非法输入、错误参数、异常状态
4. 安全测试 - SQL注入、XSS攻击、权限控制
5. 性能测试 - 响应时间、并发处理
6. 兼容性测试 - 不同浏览器、设备、分辨率
"""
```

---

## ✅ 验证方法

### 测试简单需求（1个模块）
```bash
# 上传简单需求文档（< 3000字符）
# 预期: 30-50 个测试用例
```

### 测试中等需求（2-3个模块）
```bash
# 上传中等需求文档（3000-5000字符）
# 预期: 100-300 个测试用例
```

### 测试复杂需求（5-6个模块）
```bash
# 上传 v1.2.2 需求文档（> 5000字符）
# 预期: 500-900 个测试用例
```

### 查看后端日志
```
📊 需求文档长度: 5234 字符
📊 估算模块数: 6 个
📊 目标生成: 600-900个 测试用例 (符合Skill要求)
🤖 AI 配置: max_tokens=16000, temperature=0.3
✅ AI生成成功: 新增 650 个测试用例 (使用Skill增强)
```

---

## 🚨 注意事项

### 1. AI Token 限制
- 大多数 AI 模型有 token 限制（如 4096, 8192, 16384）
- 我们设置最大 16000 tokens
- 如果需要更多，可能需要分批生成

### 2. 生成时间
- 生成 100+ 个用例需要更长时间
- 可能需要 30-60 秒
- 前端需要显示加载状态

### 3. 质量 vs 数量
- 优先保证质量
- 如果 AI 无法生成足够数量，会尽力生成
- 可以多次生成并累积

### 4. 分批生成策略
如果单次生成不够，可以考虑：
```python
# 方案 1: 按模块分批
for module in modules:
    generate_testcases_for_module(module, count=100)

# 方案 2: 按维度分批
for dimension in ['功能', '边界', '异常', '安全', '性能', '兼容性']:
    generate_testcases_by_dimension(dimension, count=50)
```

---

## 📝 更新的文件

1. `ai-test-platform/backend_api_server.py`
   - 更新模块数量估算逻辑
   - 更新生成数量计算
   - 更新 token 限制计算
   - 更新 System Prompt

2. `Skill要求更新说明.md`（本文档）
   - 说明 Skill 要求
   - 对比更新前后
   - 提供验证方法

---

## 🎉 总结

### 更新前
- ❌ 不符合 Skill 要求（每个模块100个）
- ❌ 生成数量太少（10-50个）
- ❌ 未考虑模块数量

### 更新后
- ✅ 符合 Skill 要求
- ✅ 根据模块数量动态调整
- ✅ 单模块: 30-150个
- ✅ 多模块: 可达 500-900个
- ✅ 覆盖 6 个测试维度

---

**更新完成时间**: 2024-03-24  
**状态**: ✅ 已更新并符合 Skill 要求

现在重启后端服务，上传你的 v1.2.2 需求文档，应该能生成 **500-900 个测试用例**！

