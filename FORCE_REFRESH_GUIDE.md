# 强制刷新浏览器缓存指南

## 🔴 问题

删除测试用例后,勾选状态还保留着,说明浏览器还在使用旧的JavaScript代码。

## ✅ 解决方案 (按顺序尝试)

### 方法1: 硬刷新 (最简单)

**Windows/Linux**:
- `Ctrl + Shift + R`
- 或 `Ctrl + F5`

**Mac**:
- `Cmd + Shift + R`

### 方法2: 清空缓存并硬性重新加载

1. 按 `F12` 打开开发者工具
2. **右键点击**浏览器的刷新按钮(不是左键!)
3. 选择"清空缓存并硬性重新加载"

![清空缓存](https://i.imgur.com/xxx.png)

### 方法3: 手动清除缓存

**Chrome/Edge**:
1. 按 `Ctrl + Shift + Delete`
2. 选择"缓存的图片和文件"
3. 时间范围选择"全部时间"
4. 点击"清除数据"

**Firefox**:
1. 按 `Ctrl + Shift + Delete`
2. 勾选"缓存"
3. 点击"立即清除"

### 方法4: 无痕模式测试

**Chrome/Edge**:
- `Ctrl + Shift + N`

**Firefox**:
- `Ctrl + Shift + P`

在无痕窗口中访问: http://localhost:5173/test-cases

### 方法5: 禁用缓存 (开发时推荐)

1. 按 `F12` 打开开发者工具
2. 切换到 **Network** 标签
3. 勾选 **Disable cache**
4. **保持开发者工具打开**

这样每次刷新都会重新加载所有文件。

### 方法6: 重启前端服务

如果以上都不行,重启前端服务:

```bash
# 停止前端服务 (Ctrl+C)

# 重新启动
cd ai-test-platform/frontend
npm run dev
```

## 🎯 验证是否使用新代码

### 检查1: 查看文件加载时间

1. 按 `F12` 打开开发者工具
2. 切换到 **Network** 标签
3. 刷新页面 (`F5`)
4. 找到 `index.js` 或类似的文件
5. 查看 **Time** 列,应该是刚才的时间

### 检查2: 查看源代码

1. 按 `F12` 打开开发者工具
2. 切换到 **Sources** 标签
3. 找到 `TestCasesList.jsx`
4. 搜索 `setSelectedIds([])`
5. 确认代码中有这一行

### 检查3: 测试删除功能

1. 勾选一个测试用例
2. 点击删除
3. 确认删除
4. 观察:
   - ✅ 勾选状态应该立即清除
   - ✅ 测试用例应该从列表消失
   - ✅ 不需要手动刷新

## 🔍 如果还是不行

### 添加调试代码

在浏览器控制台中运行:

```javascript
// 检查当前选中的ID
console.log('selectedIds:', window.selectedIds)

// 手动清空选中状态
// (这只是临时方案,不是永久修复)
```

### 检查前端服务

确保前端服务正在运行:

```bash
# 检查进程
netstat -ano | findstr :5173

# 如果没有运行,启动它
cd ai-test-platform/frontend
npm run dev
```

### 检查浏览器版本

确保使用的是现代浏览器:
- Chrome 90+
- Edge 90+
- Firefox 88+

## 📝 为什么会这样?

### 浏览器缓存机制

```
第一次访问
  ↓
下载 index.js (旧代码)
  ↓
缓存到本地
  ↓
后续访问
  ↓
直接使用缓存 ← 问题在这里
  ↓
不下载新代码
```

### 开发vs生产

**开发模式** (应该有热重载):
- 代码改变 → 自动刷新
- 但有时会失败

**生产模式** (需要手动刷新):
- 代码改变 → 需要清除缓存
- 或者使用版本号/hash

## 💡 最佳实践

### 开发时

1. **始终打开开发者工具**
2. **勾选 Disable cache**
3. **保持工具打开**

这样就不会有缓存问题。

### 测试时

1. **使用无痕模式**
2. **或者每次硬刷新**

### 部署时

1. **使用文件hash**: `index.abc123.js`
2. **设置Cache-Control**: 合理的缓存策略
3. **Service Worker**: 管理缓存更新

## 🎓 快速参考

| 操作 | 快捷键 | 说明 |
|------|--------|------|
| 硬刷新 | `Ctrl+Shift+R` | 忽略缓存重新加载 |
| 清除缓存 | `Ctrl+Shift+Delete` | 打开清除对话框 |
| 开发者工具 | `F12` | 打开调试工具 |
| 无痕模式 | `Ctrl+Shift+N` | 无缓存浏览 |
| 禁用缓存 | Network → Disable cache | 开发时使用 |

## ✅ 成功标志

删除测试用例后:
- ✅ 勾选状态立即清除
- ✅ 测试用例从列表消失
- ✅ 不需要手动刷新页面
- ✅ 显示正确的删除数量

## 🆘 还是不行?

如果尝试了所有方法还是不行:

1. **截图发给我**:
   - 浏览器控制台 (F12 → Console)
   - Network标签的请求
   - 删除后的页面状态

2. **提供信息**:
   - 浏览器类型和版本
   - 操作系统
   - 前端服务是否在运行

3. **尝试其他浏览器**:
   - 如果用Chrome,试试Firefox
   - 如果用Edge,试试Chrome

---

**关键操作**: `Ctrl + Shift + R` (硬刷新)

**开发建议**: F12 → Network → Disable cache (保持开发者工具打开)

**终极方案**: 无痕模式 (`Ctrl + Shift + N`)
