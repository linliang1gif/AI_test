# P2-9B.1 Windows 编码兼容修复报告

## 1. 修复了哪些文件

| 文件 | 变更内容 |
|------|---------|
| `scripts/run_regression_all.py` | 脚本开头增加 `io.TextIOWrapper` 将自身 stdout/stderr 强制 UTF-8 |
| `scripts/test_p1_7d_real_mode_safety.py` | 脚本开头增加 `io.TextIOWrapper` UTF-8 编码修复 |
| `scripts/test_p1_7e_report_persistence.py` | 同上 |
| `scripts/test_p2_6_playwright_enhanced.py` | 同上 |
| `scripts/test_p2_6b_api_performance_mvp.py` | 同上 |
| `scripts/test_p2_7_web_ui_batch_trace.py` | 同上（首轮漏修，补充修复） |

共修复 **6 个文件**，每个文件增加 4 行编码修复代码。

## 2. 是否在 run_regression_all 注入 PYTHONIOENCODING=utf-8

**是**。`run_regression_all.py` 已在 `run_script()` 的 `subprocess.run()` 中注入：

```python
env={**os.environ, "PYTHONIOENCODING": "utf-8", "TESTING": "true", ...}
```

此机制在 P2-9B 时已存在，本轮额外增加了对回归脚本自身的 stdout/stderr UTF-8 包装，确保双重保护。

保留的环境变量：`TESTING`、`TESTING_KEY`、`AI_PROVIDER`、`APP_MODE`（通过 `**os.environ` 继承）。

## 3. 是否修复旧脚本 stdout/stderr 编码

**是**。所有 5 个旧脚本 + 1 个漏修脚本，均在文件开头添加：

```python
import io, sys
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8', errors='replace')
sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding='utf-8', errors='replace')
```

此方式兼容 Windows / Linux / macOS，不依赖外部模块。

## 4. 4 个失败脚本是否全部恢复 PASS

| 脚本 | 修复前 | 修复后 | 测试数 |
|------|--------|--------|--------|
| `test_p1_7d_real_mode_safety.py` | ❌ FAIL (GBK crash) | ✅ PASS | 15/15 |
| `test_p1_7e_report_persistence.py` | ❌ FAIL (GBK crash) | ✅ PASS | 26/26 |
| `test_p2_6_playwright_enhanced.py` | ❌ FAIL (GBK crash) | ✅ PASS | 30/30 |
| `test_p2_6b_api_performance_mvp.py` | ❌ FAIL (GBK crash) | ✅ PASS | 40/40 |

额外修复：
| `test_p2_7_web_ui_batch_trace.py` | ❌ FAIL (GBK crash) | ✅ PASS | 51/51 |

> P2-7 在首轮 run_regression_all 中恰好未触发 GBK crash（通过 PYTHONIOENCODING 子进程注入），但直接运行时会崩溃，因此一并修复。

## 5. run_regression_all 最终结果

```
总计: 16  ✅ PASS: 14  ❌ FAIL: 0  ⚠️ XFAIL: 0  ⏭️ SKIP: 2
核心通过率: 14/14 = 100.0%
```

| 脚本 | 结果 | 耗时 |
|------|------|------|
| P0-7 Smoke 冒烟测试 | ✅ PASS | 17.6s |
| API Contract 契约检查 | ✅ PASS | 2.9s |
| P1-7A Import Pipeline | ✅ PASS | 78.5s |
| P1-7D Real Mode Safety | ✅ PASS | 89.2s |
| P1-7E Report Persistence | ✅ PASS | 35.6s |
| Phase 18 执行稳定性 | ✅ PASS | 165.6s |
| P2-3 Web UI Case Model | ✅ PASS | 41.8s |
| P2-4 Playwright Engine MVP | ✅ PASS | 57.5s |
| P2-5 Visual Regression MVP | ✅ PASS | 44.3s |
| P2-6 Playwright Enhanced | ✅ PASS | 115.0s |
| P2-6B API Performance MVP | ✅ PASS | 74.9s |
| P2-7 Web UI Batch/Trace | ✅ PASS | 78.6s |
| P2-8 AI UI Failure Analysis | ✅ PASS | 37.5s |
| P2-9B Web UI Stability | ✅ PASS | 41.4s |
| P1-8A AI Heal Guard | ⏭️ SKIP | 0s |
| P1-8 AI Case Review | ⏭️ SKIP | 0s |

### SKIP 说明（2 项）

| 脚本 | 原因 |
|------|------|
| P1-8A AI Heal Guard | `AI_PROVIDER=none`，无 AI 服务，预期跳过 |
| P1-8 AI Case Review | `AI_PROVIDER=none`，无 AI 服务，预期跳过 |

### XFAIL 说明

无 XFAIL。

## 6. 是否仍有 FAIL

**FAIL = 0**。无任何失败。

## 7. 是否可以进入 P2-10

**可以进入 P2-10：测试集 / 冒烟集 / 回归集管理。**

依据：
1. `run_regression_all.py` 退出码 0，FAIL=0
2. 14 个核心测试全部 PASS（100%）
3. 2 个 SKIP 为 AI Provider 未配置，预期行为
4. 0 个 XFAIL
5. 前端未涉及修改，本轮仅修复测试脚本编码，无需重新 build
6. 无业务逻辑变更、无数据库变更、无执行引擎变更

### 额外发现与修复

修复过程中发现并解决了一个 **import 冲突**问题：
- 最初创建了 `scripts/utils/` 包用于共享编码工具
- 该包会遮蔽项目根目录的 `utils/` 模块，导致 `services/__init__.py` → `auth_service.py` → `utils.auth_crypto` 导入失败
- 解决方案：删除 `scripts/utils/` 包，改用每个脚本内联的 4 行编码修复代码

---

**报告生成时间**: 2026-05-04 00:40 UTC+08:00
**基线提交**: 73c6a6f (P2-9B)
