# P2-6B.2 浏览器依赖与回归环境收口报告

**日期**: 2026-05-03
**状态**: ✅ 通过，可进入 P2-7

---

## 1. Playwright 包是否安装

**✅ 已安装。**

```
playwright 1.49.1
```

通过 `pip list | grep playwright` 确认。

## 2. Chromium 是否安装

**✅ 已安装。**

- 安装路径: `C:\Users\liolin\AppData\Local\ms-playwright\chromium-1148\chrome-win\chrome.exe`
- 版本: Chromium 131.0.6778.33
- 辅助组件: `chromium_headless_shell-1148`, `ffmpeg-1010` 均已安装

## 3. 本地是否可执行 Playwright 测试

**✅ 可以。**

通过 `check_browser_available()` 函数验证:
```python
from playwright.sync_api import sync_playwright
p = sync_playwright().start()
b = p.chromium.launch(headless=True)
b.close(); p.stop()
# → BROWSER_OK
```

三个浏览器依赖测试全部通过:
- P2-4 Playwright Engine MVP: **28/28 PASS**
- P2-5 Visual Regression MVP: **29/29 PASS**
- P2-6 Playwright Enhanced: **30/30 PASS**

## 4. CI 是否安装 Chromium

当前项目无 CI workflow 配置文件 (无 `.github/workflows` 等)。

**建议**: 未来如引入 CI，workflow 中需包含:
```yaml
- run: pip install playwright
- run: python -m playwright install chromium
```

## 5. Docker 是否支持 Playwright 浏览器

当前项目无 Dockerfile。

**建议**: 未来如引入 Docker，Dockerfile 中需包含:
```dockerfile
RUN pip install playwright && python -m playwright install --with-deps chromium
```

## 6. P2-4 PASS/XFAIL 结果

| 项目 | 状态 | 原因 |
|------|------|------|
| P2-4 Playwright Engine MVP | **✅ PASS** | Chromium 可用，28/28 测试通过 |

**之前 XFAIL 的原因**:
`run_regression_all.py` 中 P2-4 被标记为 `ext_dep="browser"`, 旧逻辑将所有 `ext_dep` 不为 None 的失败无条件转为 XFAIL。实际浏览器可用，P2-4 脚本本身也可正常执行。问题在于回归脚本的分类逻辑，而非浏览器环境。

**修复**: 新逻辑仅在 `check_browser_available()` 返回 `False` 时才 XFAIL，浏览器可用时正常 PASS/FAIL。

## 7. P2-5 PASS/XFAIL 结果

| 项目 | 状态 | 原因 |
|------|------|------|
| P2-5 Visual Regression MVP | **✅ PASS** | Chromium 可用，29/29 测试通过 |

原因同 P2-4。

## 8. P2-6A PASS/XFAIL 结果

| 项目 | 状态 | 原因 |
|------|------|------|
| P2-6 Playwright Enhanced | **✅ PASS** | Chromium 可用，30/30 测试通过 |

原因同 P2-4。

## 9. run_regression_all PASS/FAIL/XFAIL/SKIP 分类

**✅ 正确区分。**

### 核心改动

1. **动态浏览器检测**: 新增 `check_browser_available()` 函数，启动时通过 Playwright API 实际启动 Chromium 来验证可用性
2. **条件 XFAIL**: 仅当 `check_browser_available()=False` 时，浏览器依赖测试标记 XFAIL（不执行）
3. **浏览器可用时正常判定**: 浏览器可用时，P2-4/P2-5/P2-6 正常执行，结果为 PASS 或 FAIL（不自动转 XFAIL）
4. **external_api 保持原逻辑**: `ext_dep="external_api"` 的测试（Phase 18）失败仍自动转 XFAIL

### 最终回归结果

```
总计: 13  ✅ PASS: 11  ❌ FAIL: 0  ⚠️ XFAIL: 0  ⏭️ SKIP: 2
核心通过率: 11/11 = 100.0%

✅ P0-7 Smoke 冒烟测试                    PASS   18.1s
✅ API Contract 契约检查                   PASS   3.4s
✅ P1-7A Import Pipeline                   PASS   76.3s
✅ P1-7D Real Mode Safety                  PASS   94.6s
✅ P1-7E Report Persistence                PASS   35.0s
✅ Phase 18 执行稳定性                     PASS   153.1s [external_api]
✅ P2-3 Web UI Case Model                  PASS   43.7s
✅ P2-4 Playwright Engine MVP              PASS   55.6s  [browser]
✅ P2-5 Visual Regression MVP              PASS   44.4s  [browser]
✅ P2-6 Playwright Enhanced                PASS   111.2s [browser]
✅ P2-6B API Performance MVP               PASS   73.1s
⏭️ P1-8A AI Heal Guard                    SKIP   0s
⏭️ P1-8 AI Case Review                    SKIP   0s
```

## 10. Phase 18 XFAIL 表述修正

**✅ 已修正。**

之前的输出:
```
Results: 50/50 passed, 0 failed
```

修正后的输出:
```
Results: PASS=36 XFAIL=14 FAIL=0 TOTAL=50
```

改动:
- `test_phase18.py` 汇总区分 PASS / XFAIL / FAIL
- XFAIL 项单独列出，标注 `external_api timeout, not a regression`
- XFAIL 不计入 PASS
- 仅 FAIL > 0 时返回非 0 退出码

## 11. 性能测试前端安全表述修正

**✅ 已修正。**

P2-6B.1 报告中原表述:
> "前端二次确认 ✅ (handlePerfTest 中 allow_unsafe_methods: false 硬编码)"

修正为:
> "MVP 阶段前端默认禁止 unsafe performance run (allow_unsafe_methods: false 硬编码)，暂未提供二次确认放行 UI"

说明: `handlePerfTest` 中硬编码 `allow_unsafe_methods: false` 是**默认禁止**，不是**二次确认**。后续如需支持放行，应增加二次确认弹窗。

## 12. 是否可进入 P2-7

### 准入条件检查

| 条件 | 状态 |
|------|------|
| Playwright 包已安装 | ✅ 1.49.1 |
| Chromium 浏览器已安装 | ✅ 131.0.6778.33 |
| 本地可执行 Playwright 测试 | ✅ |
| P2-4 Playwright Engine MVP PASS | ✅ 28/28 |
| P2-5 Visual Regression MVP PASS | ✅ 29/29 |
| P2-6 Playwright Enhanced PASS | ✅ 30/30 |
| P2-6B API Performance MVP PASS | ✅ 40/40 |
| run_regression_all 核心通过率 100% | ✅ 11/11 |
| Phase 18 XFAIL 正确区分 | ✅ PASS=36 XFAIL=14 FAIL=0 |
| 前端安全表述准确 | ✅ |
| 前端 build 通过 | ✅ |
| 无条件 XFAIL 已消除 | ✅ |

**结论**: ✅ **可以进入 P2-7：Web UI 批量执行 / Trace 增强**

---

## 修改文件清单

| 文件 | 改动 |
|------|------|
| `scripts/run_regression_all.py` | 新增 `check_browser_available()` 动态检测；浏览器可用时 P2-4/5/6 正常 PASS/FAIL；仅 `external_api` 自动 XFAIL |
| `scripts/test_phase18.py` | 汇总改为 `PASS=x XFAIL=x FAIL=x TOTAL=x` 格式；XFAIL 不计入 PASS |
| `docs/P2-6B.1_api_performance_engineering_closure_report.md` | 修正前端安全表述 |
