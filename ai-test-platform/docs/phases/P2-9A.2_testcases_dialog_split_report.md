# P2-9A.2 TestCases 弹窗组件拆分报告

日期: 2025-05-03

## 概述

本轮继续 P2-9A.1 的组件拆分工作，将 TestCases.jsx 中的大型弹窗组件提取为独立文件，进一步降低主页面复杂度。

## 拆分前后行数

| 阶段 | TestCases.jsx 行数 |
|------|-------------------|
| P2-9A.1 拆分前 | 3742 |
| P2-9A.1 拆分后 | 3155 |
| **P2-9A.2 拆分后** | **2457** |
| **本轮减少** | **698 行 (22.1%)** |
| **累计减少** | **1285 行 (34.3%)** |

## 新增组件

| # | 组件 | 文件 | 职责 | 行数 |
|---|------|------|------|------|
| 1 | `CaseImportDialog` | `components/testcases/CaseImportDialog.jsx` | 导入/生成用例弹窗容器，含5个Tab（需求文档/Swagger文件/Swagger URL/YApi/手动创建） | ~260 |
| 2 | `ManualCaseForm` | `components/testcases/ManualCaseForm.jsx` | 手动创建用例表单（功能/API/Web UI 类型切换） | ~105 |
| 3 | `WebUICaseForm` | `components/testcases/WebUICaseForm.jsx` | Web UI 用例专用表单（登录会话、页面扫描器、快捷模板、步骤断言编辑） | ~240 |
| 4 | `AiReviewDialog` | `components/testcases/AiReviewDialog.jsx` | AI 评审结果弹窗（质量评分、风险摘要、自愈预览、覆盖缺口） | ~195 |

**Barrel export**: `components/testcases/index.js`（已更新）

## 组件层级

```
TestCases.jsx (主页面)
├── CaseImportDialog
│   ├── RequirementTab (内联)
│   ├── SwaggerFileTab (内联)
│   ├── SwaggerUrlTab (内联)
│   ├── YApiTab (内联)
│   └── ManualCaseForm
│       └── WebUICaseForm
│           ├── 登录会话管理
│           ├── 页面扫描器 (实验功能/默认折叠)
│           ├── 快捷模板区
│           ├── 步骤编辑器
│           └── 断言编辑器
├── AiReviewDialog
│   ├── 评分概览
│   ├── 改进建议
│   ├── AI 逐条问题 + 自愈
│   ├── 覆盖缺口
│   └── 规则问题明细
├── CaseDetailDialog (P2-9A.1)
├── PerfConfigDialog (P2-9A.1)
├── PerfResultDialog (P2-9A.1)
└── 其他小型弹窗 (保留在主文件)
    ├── DatasetDialog (~80行)
    ├── ScriptDialog
    ├── ResultDialog
    ├── ManualTestDialog
    └── HealDialog
```

## 各项检查

### 1. 是否拆出 CaseImportDialog

**是**。从 L1451-2090（640 行）提取为独立组件，包含全部 5 个导入 Tab。

### 2. 是否拆出 ManualCaseForm / WebUICaseForm

**是**。ManualCaseForm 封装用例类型选择、标题/模块/优先级表单、功能/API 步骤编辑；WebUICaseForm 封装登录会话、页面扫描器、快捷模板、步骤/断言编辑器。

### 3. 是否拆出 AiReviewDialog

**是**。从 L2250-2473（224 行）提取为独立组件，包含评分、建议、自愈入口、覆盖缺口等全部子区域。

### 4. 是否存在状态迁移

**否**。所有 `useState` 保留在 `TestCases.jsx`，子组件通过 props 接收数据和回调。无新 Context/Redux/Zustand。

### 5. 是否有 API 调用变化

**否**。所有 API 调用路径不变：
- `api.v2.ui.saveLoginSession` / `deleteLoginSession` / `scanPage` / `aiGenerate` 通过回调传入
- 导入接口 `handleImportRequirement` / `handleImportSwaggerFile` / `handleImportSwaggerUrl` / `handleImportYapi` 通过 props 传入
- AI 评审 `handleHealPreview` / `handleHealRetry` 通过 props 传入

### 6. XFAIL 项说明

| 项 | 说明 |
|----|------|
| **名称** | Phase 18 执行稳定性 — 批量执行超时 |
| **原因** | 批量 API 执行调用外部接口（PetStore Demo），网络延迟可能导致超时 |
| **是否外部依赖** | **是**，依赖 `petstore.swagger.io` 等外部服务 |
| **是否影响核心主链路** | **否**，超时为 XFAIL 不阻塞；本次回归实际 Phase 18 全 PASS |
| **处理方式** | 保持 XFAIL 容错机制，超时时标记为 XFAIL 而非 FAIL |

**本次回归结果**: Phase 18 全部 PASS，XFAIL=0。

### 7. 前端 build

```
✓ 4394 modules transformed
dist/assets/index-a25634c7.js   1,026.21 kB
```

**通过** ✅

### 8. run_regression_all 结果

```
总计: 15  ✅ PASS: 13  ❌ FAIL: 0  ⚠️ XFAIL: 0  ⏭️ SKIP: 2
核心通过率: 13/13 = 100.0%
```

| 测试 | 结果 | 耗时 |
|------|------|------|
| P0-7 Smoke | PASS | 17.7s |
| API Contract | PASS | 3.0s |
| P1-7A Import Pipeline | PASS | 81.8s |
| P1-7D Real Mode Safety | PASS | 87.4s |
| P1-7E Report Persistence | PASS | 36.0s |
| Phase 18 执行稳定性 | PASS | 153.9s |
| P2-3 Web UI Case Model | PASS | 42.3s |
| P2-4 Playwright Engine MVP | PASS | 50.9s |
| P2-5 Visual Regression MVP | PASS | 42.4s |
| P2-6 Playwright Enhanced | PASS | 110.9s |
| P2-6B API Performance MVP | PASS | 71.8s |
| P2-7 Web UI Batch/Trace | PASS | 76.1s |
| P2-8 AI UI Failure Analysis | PASS | 35.8s |
| P1-8A AI Heal Guard | SKIP (AI_PROVIDER=none) | — |
| P1-8 AI Case Review | SKIP (AI_PROVIDER=none) | — |

### 9. 功能验证清单

- [x] 页面不白屏
- [x] 导入/生成用例功能正常（Swagger/YApi/需求文档）
- [x] 手动创建功能/API/Web UI 用例正常
- [x] AI 评审弹窗正常
- [x] 页面扫描器保持默认折叠
- [x] 实验功能标记保留
- [x] AI_PROVIDER=none 时提示不可用
- [x] 快捷模板只生成草稿
- [x] dry_run / 自愈保护逻辑不变
- [x] API / Web UI / Visual / Performance / Failure Analysis 主链路不受影响

## 未拆分保留在主文件的弹窗

以下小型弹窗（每个 < 100 行）保留在 TestCases.jsx，不为拆而拆：

| 弹窗 | 行数 | 原因 |
|------|------|------|
| DatasetDialog | ~80 | 结构简单，状态耦合紧 |
| ScriptDialog | ~60 | 结构简单 |
| ResultDialog | ~60 | 结构简单 |
| ManualTestDialog | ~60 | 结构简单 |
| HealDialog | ~70 | 与 AiReviewDialog 状态联动 |

## P2-9A 阶段汇总

| 轮次 | 提取组件数 | 行数减少 |
|------|-----------|---------|
| P2-9A.1 | 7 | 587 (15.7%) |
| P2-9A.2 | 4 | 698 (22.1%) |
| **合计** | **11** | **1285 (34.3%)** |

TestCases.jsx: **3742 → 2457 行**，目标 ≤ 2500 行 ✅

## 结论

P2-9A.2 完成。TestCases.jsx 已从 3742 行降至 2457 行（< 2500 行目标达成），弹窗逻辑基本组件化。主文件主要负责数据加载、状态管理、事件编排、子组件组合。

**可以进入 P2-9B: Web UI 稳定性增强。**
